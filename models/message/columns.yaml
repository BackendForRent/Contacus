# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Name
        select: concat(first_name, ' ', last_name)
    email:
        label: Email
    telephone:
        label: Telephone
    message:
        label: Message
    created_at:
        label: Created at
